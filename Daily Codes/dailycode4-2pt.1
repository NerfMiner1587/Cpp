#include <iostream>
#include <Windows.h>
using namespace std;

int main() {

	int nput;

	cin >> nput;

	if (nput == 1)
		Beep(200, 10000);

	else if (nput == 2)
		Beep(1000, 10000);

	else if (nput == 3)
		Beep(10000, 10000);

}
